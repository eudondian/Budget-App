<div class="container-all">
  <nav class="navbar navbar-expand-lg navbar-light bg-light" id="user-menu">
    <% if user_signed_in? %>
      <p class="navbar-brand"><%= controller_name.titleize %></p>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item" id="sinout">
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-danger signout-btn" %>
        </li>
      </ul>
    <% else %>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_registration_path %>">Sign Up</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_session_path %>">Sign In</a>
        </li>
      </ul>
    <% end %>
  </nav>
</div>
